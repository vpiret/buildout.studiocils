[buildout]
extends =
    base.cfg

allow-picked-versions = true
show-picked-versions = true

extensions =
    mr.developer

auto-checkout =
    studiocils.policy
    studiocils.theme
    studiocils.core

[instance]
user = admin:admin
debug-mode = on
verbose-security = on
